{{page-title "Item"}}
<div class="container mx-auto px-8 py-16">
  <h1 class="text-2xl font-bold mb-10">{{this.model.item.name}}</h1>
  <p class="text-gray-600 mb-10">{{this.model.item.description}}</p>
  <div>
    <img src="{{this.model.item.image}}" alt="Art" class="w-full h-96 object-cover rounded-lg">
  </div>
</div>